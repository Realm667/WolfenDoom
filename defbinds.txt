//binds tought for a QWERTY keyboard

bind c +crouch
bind f +jump
bind g "use GrenadePickup"
bind m "openmenu MessageLogMenu"
bind o "pukename boaobjectives"
bind q "pukename QuickKick"
bind r +reload
bind space +use